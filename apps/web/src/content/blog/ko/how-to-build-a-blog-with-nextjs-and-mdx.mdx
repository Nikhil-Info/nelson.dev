---
title: Next.js와 MDX로 블로그 구축하기
date: '2022-08-22T00:00:00Z'
modifiedTime: '2025-09-16T15:11:03Z'
summary: Next.js를 사용하여 블로그를 만드는 것은 매우 쉽습니다!
---

## 서문

사용된 패키지:

- [next](https://github.com/vercel/next.js) Next.js 프레임워크
- [next-mdx-remote](https://github.com/hashicorp/next-mdx-remote) MDX 콘텐츠 처리 및 로드
- [gray-matter](https://github.com/jonschlinkert/gray-matter) 마크다운에서 front matter 파싱

이 튜토리얼의 저장소:

https://github.com/nelsonlaidev/nextjs-mdx-blog

## 데모

[라이브 데모](https://nelsonlai-nextjs-mdx-blog.vercel.app)

<Video src='/videos/blog/how-to-build-a-blog-with-nextjs-and-mdx/demo.mp4' width={1920} height={1080} />

## 블로그 만드는 방법

먼저 다음 명령어로 Next.js 프로젝트를 생성합니다:

```bash title='터미널'
yarn create next-app nextjs-mdx-blog
```

그런 다음 다음 파일들을 생성하세요:

- `components/layout.jsx` - 모든 컴포넌트를 컨테이너로 감쌈 (선택사항, 스타일 전용)
- `date/blog/*.mdx` - 블로그 글
- `lib/format-date.js` - 날짜를 `YYYY년 M월 D일` 형식으로 포맷팅
- `pages/blog/[slug].jsx` - 글 페이지, [동적 경로](https://nextjs.org/docs/pages/building-your-application/routing/dynamic-routes) 사용

<TreeView
  defaultExpandedValue={['components', 'data', 'data/blog', 'lib', 'pages', 'pages/blog']}
  collection={{
    id: 'ROOT',
    name: '',
    children: [
      {
        id: 'components',
        name: 'components',
        children: [{ id: 'components/layout.jsx', name: 'layout.jsx' }]
      },
      {
        id: 'data',
        name: 'data',
        children: [
          {
            id: 'data/blog',
            name: 'blog',
            children: [
              { id: 'data/blog/markdown.mdx', name: 'markdown.mdx' },
              { id: 'data/blog/nextjs.mdx', name: 'nextjs.mdx' },
              { id: 'data/blog/react.mdx', name: 'react.mdx' }
            ]
          }
        ]
      },
      {
        id: 'lib',
        name: 'lib',
        children: [
          { id: 'lib/format-date.js', name: 'format-date.js' },
          { id: 'lib/mdx.js', name: 'mdx.js' }
        ]
      },
      {
        id: 'pages',
        name: 'pages',
        children: [
          {
            id: 'pages/blog',
            name: 'blog',
            children: [{ id: 'pages/blog/[slug].jsx', name: '[slug].jsx' }]
          }
        ]
      }
    ]
  }}
/>

### Markdown 파일 처리 방법

먼저 `const root{:js}` - 루트 디렉토리, `process.cwd(){:js}` 메서드는 Node.js 프로세스의 현재 작업 디렉토리를 반환합니다.

```js title='lib/mdx.js'
const root = process.cwd()
```

다른 변수 `POSTS_PATH{:.entity.name.constant}`는 글 파일이 저장된 경로입니다.

```js title='lib/mdx.js'
import path from 'path'

const POSTS_PATH = path.join(root, 'data', 'blog') // [!code highlight]
// 출력: A:/nextjs-mdx-blog/data/blog
```

그런 다음 fs를 사용하여 이 디렉토리의 내용, 즉 `data/blog`에 있는 모든 파일 이름을 읽습니다.

```js title='lib/mdx.js'
import fs from 'fs'

export const allSlugs = fs.readdirSync(POSTS_PATH)
// 출력: ['markdown.mdx', 'nextjs.mdx', 'react.mdx']
```

그런 다음 나중에 사용될 파일 확장자를 제거하는 함수를 작성하세요.

```js title='lib/mdx.js'
export const formatSlug = (slug) => slug.replace(/\.mdx$/, '')
/*
 * 예시: formatSlug('markdown.mdx')
 * 출력: 'markdown'
 */
```

다음 단계는 slug로 글 내용을 가져오는 것입니다.

```js title='lib/mdx.js'
export const getPostBySlug = async (slug) => {
  const postFilePath = path.join(POSTS_PATH, `${slug}.mdx`)
  // 출력: A:/nextjs-mdx-blog/data/blog/slug.mdx
  const source = fs.readFileSync(postFilePath)
  // 파일 내용 반환

  const { content, data } = matter(source)
  /*
   * 예시:
   *  ---
   *  title: 안녕하세요
   *  slug: home
   *  ---
   *  <h1>Hello world!</h1>
   *
   * 반환:
   *  {
   *    content: '<h1>Hello world!</h1>',
   *    data: {
   *      title: '안녕하세요',
   *      slug: 'home'
   *    }
   *  }
   */

  const mdxSource = await serialize(content)
  // 처리할 내용을 직렬화(next-mdx-remote) 위해 넣음

  const frontMatter = {
    ...data,
    slug
  }
  // 나중에 사용될 slug도 front matter에 넣음

  return {
    source: mdxSource,
    frontMatter
  }
}
```

그런 다음 홈페이지에 표시할 모든 글을 가져올 수 있습니다.

```js title='lib/mdx.js'
export const getAllPosts = () => {
  const frontMatter = []

  allSlugs.forEach((slug) => {
    const source = fs.readFileSync(path.join(POSTS_PATH, slug), 'utf-8')

    const { data } = matter(source)

    frontMatter.push({
      ...data,
      slug: formatSlug(slug),
      date: new Date(data.date).toISOString()
    })
  })

  return frontMatter.sort((a, b) => dateSortDesc(a.date, b.date))
}

// 날짜 기준 내림차순 정렬
const dateSortDesc = (a, b) => {
  if (a > b) return -1
  if (a < b) return 1

  return 0
}
```

### 날짜 포맷팅

```js title='lib/format-date.js'
export const formatDate = (date) =>
  new Date(date).toLocaleDateString('ko', {
    year: 'numeric',
    month: 'long',
    day: 'numeric'
  })
/*
 * formatDate('2022-08-21T00:00:00Z')
 * 출력: '2022년 8월 21일'
 */
```

### 홈페이지

```jsx title='pages/index.jsx'
import { formatDate } from '../lib/format-date'
import { getAllPosts } from '../lib/mdx'

import Link from 'next/link'

export default function Home({ posts }) {
  return (
    <>
      <h1 className='mb-8 text-6xl font-bold'>블로그</h1>
      <hr className='my-8' />
      <ul className='flex flex-col gap-3'>
        {posts.map(({ slug, title, summary, date }) => (
          <li key={slug}>
            <Link href={`/blog/${slug}`}>
              <a className='block rounded-lg border border-solid border-gray-300 p-6 shadow-md'>
                <div className='flex justify-between'>
                  <h2>{title}</h2>
                  <time dateTime={date}>{formatDate(date)}</time>
                </div>
                <p className='mt-4'>{summary}</p>
              </a>
            </Link>
          </li>
        ))}
      </ul>
    </>
  )
}

// getStaticProps를 사용하여 모든 글 가져오기
export const getStaticProps = async () => {
  const posts = getAllPosts()

  return {
    props: {
      posts
    }
  }
}
```

### 글 페이지

```jsx title='pages/[slug].jsx'
import { formatDate } from '../../lib/format-date'
import { allSlugs, formatSlug, getPostBySlug } from '../../lib/mdx'

import { MDXRemote } from 'next-mdx-remote'

export default function Blog({ post }) {
  const { title, date } = post.frontMatter

  return (
    <div>
      <h1 className='mb-2 text-6xl font-bold'>{title}</h1>
      <time dateTime={date} className='text-lg font-medium'>
        {formatDate(date)}
      </time>
      <hr className='my-8' />
      <article className='prose max-w-none'>
        <MDXRemote {...post.source} />
      </article>
    </div>
  )
}

export const getStaticProps = async ({ params }) => {
  const post = await getPostBySlug(params.slug)

  return {
    props: {
      post
    }
  }
}

export const getStaticPaths = async () => {
  const paths = allSlugs.map((slug) => ({
    params: {
      slug: formatSlug(slug)
    }
  }))
  /*
   * paths 출력:
   *   [
   *     { params: { slug: 'markdown' } },
   *     { params: { slug: 'nextjs' } },
   *     { params: { slug: 'react' } }
   *   ]
   */

  return {
    paths,
    fallback: false
  }
}
```

이렇게 하면 간단한 블로그가 완성됩니다.

## 유용한 링크

- [getStaticPaths](https://nextjs.org/docs/pages/building-your-application/data-fetching/get-static-paths)
- [getStaticProps](https://nextjs.org/docs/pages/building-your-application/data-fetching/get-static-props)
- [동적 경로](https://nextjs.org/docs/pages/building-your-application/routing/dynamic-routes)
- [mdx](https://mdxjs.com/)